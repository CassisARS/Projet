

{% extends 'base.html.twig' %}
{% block title %} Test {% endblock %}

    {% block header %}
        AVANT
        {{ parent() }}
        APRES
    {% endblock %}

{% block body %}
    <h1>Page test</h1>


    {# Création d'une variable #}
    {% set firstName = "GROOOOOOOOOT" %}
    <h2>Je s'appelle {{ firstName }} !!!!!!!!!!</h2>
    <p>Il y a {{ colors|length }} couleurs dans mon tableau de couleurs.</p>
    <p>{{ colors|join(', ') }}</p>
    <p>Les couleurs de ma variable sont : </p>
    <ul>
        {% for color in colors %}
            <li>{{ color }}</li>
            {% if not loop.last %}
                <hr>
            {% endif %}
        {% endfor %}
    </ul>
    {{ dump(colors) }}
    <p>{{ today|date("Y") }}</p>


{% endblock %}